# Repository Cleanup — 2026-09-19

Checkpoint before cleanup:

- `checkpoint/pre-repo-cleanup-20260919`
- source commit: `18fe5d7`

Quarantine location:

- `D:\NeginAI-Quarantine\20260919-1207`

## Removed from the active project tree without destructive deletion

The following categories were moved to quarantine or pruned after a verified copy:

- accidental root garbage files created by shell/patch commands,
- temporary Base64 patch files,
- generated root inventory dumps,
- old root EXE build outputs,
- `canonical.zip`,
- old root Uvicorn logs,
- `.tmp/` one-shot SQL/accounting probe scripts,
- root `reports/` containing non-project generated CSV output,
- pytest runtime/cache directories,
- old Codex remote attachments,
- Drive transfer scratch,
- `.runtime/` diagnostics/build logs/benchmarks after a byte-for-byte quarantine copy.

Empty untracked directories left by previous experiments under Android/skills were removed.

## Intentionally retained

These were not treated as junk because they can still be useful or are required for operation:

- `.env`
- `.venv/`
- `node_modules/`
- `data/`
- `logs/`
- `artifacts/`
- `checkpoints/`
- `visualizations/`
- current tracked source, tests, scripts, docs, skills, mobile projects, and ops files.

## Policy added

- Root `.tmp/` is disposable scratch.
- Root `reports/` is generated/non-project output; canonical reports belong under `docs/reports/`.
- See `docs/PROJECT_STRUCTURE.md` for the canonical repository layout.

Nothing in this cleanup changes Varanegar data, backend business logic, frontend behavior, or production database contents.
