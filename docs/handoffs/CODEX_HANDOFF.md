# تحویل جاری پروژه NeginAI به Codex روی سیستم دیگر

تاریخ تهیه: ۱۴۰۵/۰۶/۰۹ ـ 2026-08-31  
مسیر پروژه: `G:\NeginAI`

> این فایل مرجع اصلی ادامه کار در یک چت یا کامپیوتر جدید است. فایل‌های
> `docs/HANDOFF_FA.md`، `CURRENT_STAGE.md` و
> `docs/CHECKPOINT_PREVISIT_HANDOFF_FA.md` اطلاعات تاریخی مفیدی دارند، اما مرحله
> جاری پروژه را کامل منعکس نمی‌کنند.

## متن آماده برای شروع چت جدید

این متن را در Codex سیستم دوم بفرستید:

```text
پروژه G:\NeginAI را ادامه بده. ابتدا فایل G:\NeginAI\CODEX_HANDOFF.md را کامل
بخوان، سپس فقط برای شناخت وضعیت فعلی فایل‌ها و تست‌های ارجاع‌شده در آن را بررسی
کن. هیچ سرویس زنده، دیتابیس عملیاتی، تنظیم شبکه، فایل .env یا نسخه نصب‌شده را بدون
درخواست صریح من تغییر نده. تغییرات موجود کاربر را حفظ کن و کار را از بخش «اولویت
ادامه» پیش ببر.
```

## هدف فعلی محصول

محصول فعلی یک اپ حرفه‌ای برای بازاریاب است که باید در نهایت جای رابط موبایل NGT
را بگیرد، ولی فعلاً تنظیمات، اجبارها، محاسبه قیمت/مالیات/تخفیف/جایزه و مسیر رسمی
انتقال عملیات را از NGT Server دریافت کند.

معماری مورد توافق:

```text
اپ وب و Android بازاریاب
        ↓
NeginAI Backend
        ↓
NGT Web API (قوانین، محاسبه، Tour/CustomerCall، savedata)
        ↓
Replicate کنترل‌شده
        ↓
Varanegar
```

اصل مهم: برای سفارش، ویرایش مشتری، ثبت موقعیت و تعریف مشتری جدید نباید نوشتن
مستقیم و بدون قرارداد در جداول عملیاتی وارانگار جایگزین مسیر رسمی NGT شود.

## وضعیت کلی فعلی

### بخش‌های ساخته‌شده و قابل تست

- ورود بازاریاب و شناسایی فروشنده.
- «تورهای ویزیت» و انتخاب مسیر روز.
- فهرست مشتریان مسیر با ورود مستقیم به ویزیت یا ثبت عدم ویزیت.
- نمایش نام مشتری، نام فروشگاه و آدرس خوانا در فهرست مسیر.
- صفحه مشتری با منوی سه‌نقطه‌ای و بخش‌های اطلاعات مشتری، سفارش‌گیری، اطلاعات
  مالی، فاکتورهای باز، علایق/سابقه خرید و درخواست‌های ذخیره‌شده.
- شروع ویزیت از فهرست مشتریان بدون صفحه انتخاب تکراری مسیر/مشتری.
- اطلاعات تحلیلی مشتری: کاردکس، مانده، فاکتور باز، چک برگشتی، تاریخچه خرید،
  برندها و آخرین فروشنده در حد داده موجود.
- سفارش‌گیری از «لیست کالا و صوت» و «کاتالوگ گروهی».
- فیلتر برند و گروه کالا؛ گروه‌ها باید به برند انتخاب‌شده وابسته باشند.
- مرتب‌سازی کالا بر اساس برند، گروه و کوچک‌ترین گروه.
- سفارش چندواحدی بر اساس واحدهای موجود هر کالا مانند عدد، بسته و کارتن.
- انتخاب نوع درخواست، روش پرداخت و انبار مجاز فروشنده.
- موجودی وابسته به انبار و جلوگیری رابط کاربری از سفارش بیش از موجودی.
- پیش‌نمایش رسمی قیمت، مالیات، تخفیف و جایزه از موتور NGT/EVC.
- سبد شناور کوچک، پیش‌فاکتور، ویرایش مقادیر، ذخیره چند درخواست مستقل و PDF.
- کاتالوگ گروهی با تصویر و کالاهای متصل به هر تصویر.
- موتور سفارش صوتی داخلی و واژه‌نامه کالا؛ دکمه صوتی باید کوچک و دم‌دست باشد،
  نه یک تب مستقل و بزرگ.
- نقشه و مسیریابی نشان؛ جزئیات GPS و «حرکت به پایگاه» باید داخل بخش نقشه باشد،
  نه در فهرست مشتریان.
- پوسته وب مشکی/طلایی؛ میز نمایش کاتالوگ با زمینه سفید/سبز برای خوانایی تصاویر.
- Android یک WebView/پوسته بومی با GPS، میکروفون، دانلود، چاپ/PDF و مسیریابی دارد.

### نسخه‌های Frontend در لحظه تهیه این فایل

```text
assistant.css?v=106
previsit-workspace.css?v=50
assistant.js?v=213
service worker shell: neginai-shell-v214
```

این شماره‌ها فقط برای تشخیص Cache هستند. پس از تغییر فایل‌های استاتیک باید نسخه
ارجاع و Cache هماهنگ افزایش یابد و روی موبایل با Reload واقعی بررسی شود.

## تصمیم‌های قطعی تجربه کاربری

1. عنوان مسیر روز در محصول «تورهای ویزیت» است.
2. صفحه تور در حالت فهرست فقط مشتریان را نشان می‌دهد؛ نام، فروشگاه و آدرس کافی
   است و امتیازهای شلوغ و کنترل GPS در این صفحه نمایش داده نشوند.
3. روی هر مشتری دو اقدام اصلی وجود دارد: «ورود به ویزیت» و «عدم ویزیت».
4. ورود به ویزیت باید Loading واضح داشته باشد و مستقیماً صفحه همان مشتری را باز
   کند؛ صفحه واسط انتخاب مجدد مسیر/مشتری ممنوع است.
5. ورود اولیه به صفحه مشتری روی «اطلاعات مشتری» انجام می‌شود.
6. اگر NGT فیلد یا موقعیت اجباری تعیین کرده باشد، سفارش‌گیری تا تکمیل آن قفل است.
7. فقط فیلدهایی که تنظیمات NGT اجازه می‌دهد قابل ویرایش باشند؛ بقیه فقط‌خواندنی‌اند.
8. ثبت موقعیت، نقشه را باز می‌کند، GPS می‌گیرد و طول/عرض را خودکار درج می‌کند؛
   ارسال نهایی آن باید از قرارداد رسمی `CustomerLocations` انجام شود.
9. تعیین تکلیف مشتری الزامی است: حداقل یک درخواست ذخیره‌شده/ثبت‌شده، یا عدم
   سفارش، سپس پایان ویزیت. عدم ویزیت از فهرست تور ثبت می‌شود.
10. پس از تعیین تکلیف، در فهرست مشتریان نشان واضحی برای سفارش، بدون سفارش یا عدم
    ویزیت نمایش داده می‌شود.
11. «شرایط سفارش» در بالای صفحه فضای ثابت بزرگ نگیرد؛ مقادیر اصلی قابل تغییر
    باشند و فهرست کالا بیشترین فضای صفحه را داشته باشد.
12. فیلتر برند/گروه هنگام شروع اسکرول جمع شود ولی در نوار کشویی سریع قابل بازشدن
    باشد.
13. دکمه صوتی در منوی مستقل نمایش داده نشود؛ فقط دکمه کوچک کنار صفحه/سبد باشد.
14. وب و Android باید تا جای ممکن یک UI و یک منطق مشترک داشته باشند.
15. نام NGT در متن‌های کاربری دیده نشود؛ NGT یک جز فنی پشت صحنه است.

## چرخه مطلوب ویزیت

```text
تورهای ویزیت
  → انتخاب تور
  → فهرست مشتریان
      ├─ عدم ویزیت + دلیل فعال خوانده‌شده از NGT
      └─ ورود به ویزیت
           → شروع Timer و ساخت CustomerCall رسمی
           → اطلاعات مشتری
           → تکمیل فیلد/موقعیت اجباری
           → آزاد شدن منوی سفارش‌گیری
           → یک یا چند درخواست ذخیره‌شده یا عدم سفارش
           → پایان ویزیت
           → بازگشت به فهرست و نمایش وضعیت قطعی مشتری
```

## بررسی کامل انجام‌شده روی NGT

نسخه بررسی‌شده NGT: `5.9.0.170`

منابع بررسی شامل اسمبلی‌های Client/Server، مدل‌های Sync، Controllerها، Business
Logic، تنظیمات زنده و قراردادهای دیتابیس بوده‌اند. نتیجه کلیدی:

- NGT فقط صفحه سفارش نیست؛ موتور وضعیت Tour و CustomerCall است.
- مدل اصلی ارسال `SyncGetTourViewModel` است.
- ثبت رسمی از این مسیر انجام می‌شود:

```text
POST /api/v2/ngt/tour/sync/savedata
```

- انتقال پیش‌فروش به وارانگار از Replicate کنترل‌شده انجام می‌شود:

```text
GET /api/v2/ngt/tour/PreSale/Replicate/{tourId}
```

- ویرایش مشتری در `CustomerUpdates`، موقعیت در `CustomerLocations` و مشتری جدید
  در `CustomerCalls[].SyncCustomer` قرار می‌گیرد.
- هر تلاش ویزیت باید CustomerCall مستقل داشته باشد؛ `Tour + Customer` به‌تنهایی
  کلید یکتا نیست.
- NGT علاوه بر سفارش، پرداخت/چک، مرجوعی، پرسش‌نامه، تصویر، موجودی‌گیری مشتری،
  مشاهده کاتالوگ و وضعیت‌های ارسال/دریافت/لغو/تأیید را مدیریت می‌کند.
- نگاشت سفارش NGT به سفارش وارانگار همیشه یک‌به‌یک نیست؛ Split و Merge ممکن است.

## شکاف‌های مهم و باز

این موارد ظاهر UI نیستند و قبل از استفاده عملیاتی گسترده باید حل شوند.

### P0 ـ صحت عملیاتی

1. هویت رسمی Tour و CustomerCall هنوز در کل چرخه اپ مبنای قطعی نیست.
2. پایان ویزیت و تیک فهرست مشتریان عمدتاً از وضعیت محلی SQLite به دست می‌آید، نه
   رسید قطعی NGT.
3. ارسال کامل `savedata → receipt → Replicate → Varanegar ID` هنوز مسیر فعال و
   قابل Reconcile محصول نیست.
4. Bridge مستقیم وارانگار مسیر نهایی مورد قبول نیست و سفارش دارای جایزه را نیز
   کامل پشتیبانی نمی‌کند.
5. چند درخواست ذخیره‌شده باید هرکدام مستقل، روی سرور دوباره محاسبه و به سفارش
   مستقل تبدیل شوند؛ رفتار نهایی این بخش هنوز نیازمند اصلاح و تست است.
6. Preview ارسالی مرورگر نباید مرجع ذخیره باشد؛ قیمت/مالیات/تخفیف/جایزه باید قبل
   از ذخیره در Backend دوباره از مرجع رسمی محاسبه و امضا/نسخه‌گذاری شود.
7. UUIDهای `EditCustomerFields` باید از `DeviceSettingKeyTypes` به نام فیلد واقعی
   Resolve شوند. روشن بودن `AllowEditCustomer` نباید همه فیلدها را قابل ویرایش کند.
8. ویرایش مشتری و ثبت موقعیت فعلاً Draft محلی‌اند و باید با قرارداد رسمی ارسال،
   رسید و Retry شوند.
9. Outbox آفلاین، Idempotency، Retry، Conflict handling و بازیابی عملیات قطع‌شده
   هنوز در حد NGT وجود ندارد.
10. Actor واقعی بازاریاب و نسخه تنظیمات اعمال‌شده باید در هر فرمان و رسید ثبت شود.

### P1 ـ برابری عملیاتی با NGT

- تعریف مشتری جدید.
- عدم سفارش/عدم ویزیت رسمی با دلایل فعال NGT.
- ساعات کاری، مراجعه خارج از مسیر و سقف آن، Single Send و وضعیت Tour.
- Order Approval، Price Class و Split by Supplier.
- وصول نقد/چک/صیاد و تخصیص آن به اسناد.
- مرجوعی و کالای جایگزین.
- پرسش‌نامه و تصویر اجباری.
- موجودی‌گیری کالای داخل فروشگاه مشتری.
- ثبت دفعات و مدت مشاهده کاتالوگ.
- لغو، بازگشایی، تأیید مجدد و Replicate مجدد همراه Audit trail.

## تنظیمات NGT و اصل پیاده‌سازی

تنظیمات NGT ساده و تک‌جدولی نیستند. حداقل پنج لایه تقدم و تعداد زیادی Device/App
Setting دارند. هیچ اجبار مهمی نباید صرفاً در JavaScript Hard-code شود.

برای هر Visit یک Policy Snapshot تولید شود که حداقل شامل این موارد باشد:

- شناسه و نسخه تنظیمات مؤثر.
- فیلدهای قابل ویرایش و اجباری مشتری.
- اجبار ثبت موقعیت و حداکثر فاصله.
- دلایل فعال عدم سفارش و عدم ویزیت.
- انبار، نوع درخواست، روش پرداخت، Price Class و مجوزهای بازاریاب.
- مجوز مشتری خارج از مسیر و سقف آن.
- کنترل موجودی و روش محاسبه موجودی قابل فروش.
- ساعات کاری و کنترل‌های پایان و ارسال تور.

## نکات فنی فعلی

### Backend و سرویس‌ها

```text
app/main.py                         راه‌اندازی FastAPI
app/config.py                       تنظیمات محیطی
app/routes/seller_workspace.py      APIهای تور، مشتری، سفارش و درخواست ذخیره‌شده
app/seller_workspace_service.py     داده و Policy میزکار بازاریاب
app/previsit_service.py             Visit/Draft/Request محلی
app/ngt_previsit_service.py         خواندن قوانین و پیش‌نمایش NGT
app/varanegar_order_bridge.py       Bridge آزمایشی؛ مسیر نهایی رسمی نیست
app/static/assistant.html           Shell رابط
app/static/assistant.js             منطق اصلی رابط بازاریاب
app/static/assistant.css            پوسته عمومی و تم مشکی/طلایی
app/static/previsit-workspace.css   طراحی میزکار و سفارش‌گیری
app/static/service-worker.js        Cache پوسته و تصاویر کاتالوگ
```

### Android

```text
android/SellerNavigator
```

Android از URL اصلی `https://ai.neginpakhsh.com` استفاده می‌کند و بخش عمده UI
را از همان Web App می‌گیرد. تغییرات Web باید روی WebView واقعی Android، دکمه Back،
Safe Area پایین، میکروفون، GPS، دانلود و Print/PDF مجدداً بررسی شوند.

### دیتابیس محلی

```text
data/neginai.db
```

این فایل حاوی داده عملیاتی محلی است. بدون توقف سرویس و نسخه پشتیبان جایگزین یا
حذف نشود.

## مستندات مرجع که باید خوانده شوند

برای ادامه کار مرتبط با ویزیت و سفارش، این فایل‌ها به‌ترتیب اهمیت خوانده شوند:

```text
docs/NGT_CUSTOMER_SYNC_CONTRACT_FA.md
docs/VARANEGAR_ORDER_BRIDGE_FA.md
docs/CHECKPOINT_PREVISIT_HANDOFF_FA.md
docs/varanegar_reconstruction/NGT_CONFIGURATION_PRECEDENCE_AND_TRANSPORT_BOUNDARY_20260829_FA.md
docs/varanegar_reconstruction/NGT_TOUR_CUSTOMER_CALL_STATE_AND_COMMAND_BOUNDARY_20260829_FA.md
docs/varanegar_reconstruction/NGT_ORDER_PERSISTENCE_REPLICATION_AND_CROSSWALK_BOUNDARY_20260829_FA.md
docs/varanegar_reconstruction/NGT_PAYMENT_SETTLEMENT_AND_RECEIPT_BOUNDARY_20260829_FA.md
```

## تست و راه‌اندازی

راه‌اندازی عادی Backend طبق README:

```powershell
Set-Location G:\NeginAI
.\start.bat
```

اجرای تست‌های عمومی:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

تست‌های متمرکز جریان بازاریاب:

```powershell
.\.venv\Scripts\python.exe -m pytest -q `
  tests\test_seller_workspace.py `
  tests\test_previsit_day_route.py `
  tests\test_previsit_saved_requests.py `
  tests\test_ngt_previsit.py `
  tests\test_varanegar_order_bridge.py `
  tests\test_android_distribution.py

node --test tests\grouped_catalog_ui.test.cjs
node --test tests\order_voice_engine.test.cjs
node --test tests\previsit_invoice_units.test.cjs
```

آخرین بررسی متمرکز قبل از این فایل: تست‌های JavaScript سه بخش فوق موفق بودند؛
مجموعه Python مرتبط در بررسی قبلی ۱۱۷ از ۱۱۸ تست را پاس کرد و یک Regression در
پایان ویزیت با «درخواست ذخیره‌شده + سبد جاری خالی» باقی مانده بود. این نتیجه باید
در سیستم جدید تازه اجرا شود و به‌عنوان وضعیت همیشگی فرض نشود.

## محدودیت‌ها و احتیاط‌های محیط

- ریشه `G:\NeginAI` در زمان تهیه این فایل Git repository تشخیص داده نشد؛ بنابراین
  قبل از هر تغییر بزرگ Snapshot یا نسخه پشتیبان گرفته شود و به `git status` تکیه
  نشود.
- فایل `.env` محرمانه است؛ محتوای آن را در چت، مستندات یا خروجی تست چاپ نکنید.
- رمز کاربران، SQL، OpenAI، روتر، NGT و سایر کلیدها نباید به این فایل افزوده شوند.
- دسترسی شبکه به پوشه پروژه، تاریخچه چت Codex را منتقل نمی‌کند؛ این فایل برای
  انتقال دانش پروژه است، نه Session داخلی Codex.
- هیچ Restart سرویس IIS/NGT/SQL، تغییر تنظیمات سرور 171/173، NAT، Caddy، انتشار
  APK، Commit یا Replicate واقعی بدون درخواست صریح کاربر انجام نشود.
- نوشتن روی دیتابیس زنده NGT/Varanegar فقط پس از تعیین دقیق Target، تهیه Backup،
  تأیید مسیر رسمی و مجوز صریح انجام شود.

## اولویت ادامه پیشنهادی

اولین کار فنی بعدی باید یک Vertical Slice واقعی و قابل Reconcile باشد:

1. Resolve یا ایجاد Tour رسمی برای مسیر انتخاب‌شده.
2. ایجاد CustomerCall رسمی هنگام ورود به ویزیت.
3. Snapshot کردن Policy مؤثر NGT برای همان بازاریاب/مشتری/ویزیت.
4. تکمیل Gate اطلاعات و موقعیت اجباری.
5. محاسبه مجدد سروری هر درخواست ذخیره‌شده.
6. ساخت `SyncGetTourViewModel` و ارسال Pilot به `savedata` با Idempotency.
7. نگهداری رسید، وضعیت و خطای قابل Retry.
8. Replicate کنترل‌شده و ثبت شناسه‌های NGT و وارانگار در Crosswalk.
9. نمایش وضعیت قطعی همان CustomerCall در فهرست مشتریان.

تا قبل از تکمیل این Slice، بهبودهای ظاهری گسترده نباید جای اتصال عملیاتی را بگیرد.

