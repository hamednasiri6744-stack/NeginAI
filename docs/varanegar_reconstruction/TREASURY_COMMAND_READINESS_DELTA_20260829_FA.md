# دلتا‌ی آمادگی فرمان خزانه — ۱۴۰۵/۰۶/۰۷

بعد از قرارداد ۱۲فرمانی outcome/retry، ماژول `receivables_treasury` از نظر طراحی `target_contract=false` به `true` تغییر کرد. شمار ماژول‌های دارای قرارداد طراحی‌شده از چهار به پنج رسید.

این تغییر فقط پوشش طراحی است. runtime retry/idempotency، command-ready و pilot-ready همگی صفر باقی ماندند. ثبت ریسک ۸۴ و traceability پایه ۳۴۳ تغییر نکرد.
