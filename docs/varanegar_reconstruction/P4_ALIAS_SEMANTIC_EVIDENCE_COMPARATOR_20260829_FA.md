# Comparator معنایی Candidateهای P4 ـ ۲۰۲۶-۰۸-۲۹

سه Candidate export/read به ۱۴ جفت Case هم‌نوع نرمال‌شده تبدیل شد: Authorization سه، Failure Injection شش، Idempotency دو و Success سه جفت. هر سه Candidate دست‌کم یک جفت قابل‌مقایسه دارد؛ سطح Read بانکی فقط دو جفت Authorization/Success تولید می‌کند.

Precondition، outcome، assertion-list و full exact همگی صفر و Result parity proven نیز صفر است. expected-array پایه فقط assertion محسوب شده و نبود outcome label به تطابق ساختگی تبدیل نشده است.

Read access، Export artifact و Frozen-fixture result parity سه قرارداد جدا هستند. Candidate/Case accepted، اجرا، additive و readiness صفر و lower bound طراحی ۱۴۰۴ باقی ماند.
