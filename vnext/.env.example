# NeginAI vNext frontend runtime contract.
# Same-origin is the default and requires no value.
VITE_API_BASE_URL=

# Client-side request timeout only. Does not alter backend/business timeouts.
VITE_API_TIMEOUT_MS=15000
